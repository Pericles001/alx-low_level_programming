#include <stdio.h>
/**
 * main - the function
 * argc: the argument size
 * argv: the argument length
 * Return: 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	
	return (0);
}
